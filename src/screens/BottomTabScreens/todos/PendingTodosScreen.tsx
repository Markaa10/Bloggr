import React from 'react';
import {StyleSheet, Text, View} from 'react-native';

const PendingTodosScreen = () => {
  return (
    <View>
      <Text>PendingTodosScreen</Text>
    </View>
  );
};

export default PendingTodosScreen;

const styles = StyleSheet.create({});
